{
  "name": "duplicate-tab-shortcut",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that duplicates the selected tab(s) with a keyboard shortcut.",
  "scripts": {
    "package": "./scripts/package-extension.sh",
    "zip": "npm run package",
    "icons": "./scripts/generate-icons.sh",
    "test": "node --test tests/*.test.js"
  }
}
